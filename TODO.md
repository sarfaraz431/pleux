# TODO

- [x] Add multi-image support to `Product` type (`images?: string[]`) with backward compatibility.

- [x] Update Admin `ProductManager` to upload multiple images and save as `images: string[]` (keep `image` as first image).


- [ ] Update `ProductDetails` page UI to show premium gallery (main image + responsive thumbnails).
- [ ] Add per-product SEO meta tags (title/description/canonical/OG/twitter) and JSON-LD product schema in `ProductDetails`.
- [ ] Run `npm run build` (and/or `npm run dev`) to ensure TS/React code compiles.

